const githubConfig = require('./github.conf');
const globalConfig = require('./global.conf');
const databaseConfig = require('./db.conf');
const logConfig = require('./log.conf');
const jwtConfig = require('./jwt.conf');

module.exports = {
  githubConfig,
  globalConfig,
  databaseConfig,
  logConfig,
  jwtConfig
}